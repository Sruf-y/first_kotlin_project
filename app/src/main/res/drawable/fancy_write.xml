<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Transparent Background -->
    <item>
        <shape android:shape="rectangle">
        </shape>
    </item>

    <!-- Underline with rounded corners -->
    <item
        android:bottom="10dp"
        android:left="7dp"
        android:right="7dp"
        android:top="67.5dp">

        <shape android:shape="rectangle">
            <size android:height="1dp" />
            <corners android:radius="50dp" />
            <solid android:color="@android:color/black" /> <!-- Use @android:color/black -->
        </shape>
    </item>
</layer-list>
